{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.11",
    "node": "20"
  },
  "deploy": {
    "startCommand": "npm install && playwright install --with-deps chromium && python bot.py"
  }
}
